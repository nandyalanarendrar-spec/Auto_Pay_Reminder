# This module has been deprecated.
# Phone number OTP verification is now handled entirely by Supabase's built-in
# signInWithOtp / verifyOtp email system on the frontend (same as registration).
# No custom backend OTP service is needed.
